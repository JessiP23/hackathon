class NotifyService:
    def notify_vendor_order(self, vendor_phone: str, message: str):
        # iMessage API stub (replace with actual iMessage MCP)
        print(f"[Vendor iMessage] To {vendor_phone}: {message}")